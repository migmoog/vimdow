[project]
name = "vimdow"
version = "0.3.5"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "commented-configparser>=3.0.0",
    "invoke",
]
