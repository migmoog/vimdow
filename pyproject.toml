[project]
name = "vimdow"
version = "0.4.0"
description = "Neovim client based on godot."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "commented-configparser>=3.0.0",
    "invoke",
    "python-dotenv>=1.2.2",
]
